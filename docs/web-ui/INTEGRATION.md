# Web UI Document Integration

PDF/document processing can be exposed to the Chimera II Web UI through a sandboxed adapter. The Aurora desktop may provide document browsing, extraction, search, and preview surfaces without granting browser code unrestricted filesystem access. The integration hub is `amerhwitat/test` and the canonical OS integration is `amerhwitat/ChimeraIIOS`.
