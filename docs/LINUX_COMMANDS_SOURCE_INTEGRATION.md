# Linux Commands Source Integration

Use GNU Coreutils, util-linux, iproute2/net-tools, sudo, POSIX shells, and Toybox as documented upstream references. Preserve source licenses and SPDX metadata; implement project-specific integration through adapters.

Performance policy: parallelize independent document/page work with bounded workers while keeping GUI operations on the GUI thread and preserving deterministic output ordering.

Canonical standalone and Aurora Web UI integration is maintained in `amerhwitat/ChimeraIIOS`.
