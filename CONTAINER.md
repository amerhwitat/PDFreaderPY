# Containerized version

```bash
docker compose build
docker compose up
```

The image is a non-root Python runtime. If the repository exposes a web/service mode it is available on port 8000; native desktop usage remains outside Docker.
