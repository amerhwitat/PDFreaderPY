# Apple application

Native SwiftUI/Xcode shell for PDFreaderPY. PDF/document ingestion remains a portable service boundary; Python is not assumed to be embedded in the IPA. Use native Apple document APIs or a separately deployed processing service when required.

Targets: iOS/iPadOS and macOS. IPA export requires macOS/Xcode.
