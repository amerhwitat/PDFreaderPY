# C#/.NET Thamudic bridge

Modern C# integration target for PDF/image extraction pipelines. The canonical core is multi-targeted for .NET 8, 9 and 10 so the same API can be consumed by current LTS and STS runtimes.
